[project]
name = "SimpleVisualizer"
version = "0.1.0"
description = "Lokalni Simple visualizer"
dependencies = ["pyyaml"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"